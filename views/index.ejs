<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Note.MEE</title>
    <link rel="stylesheet" href="/stylesheets/index.css" />
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>

</head>

<body>
    <div class="main w-full  min-h-screen bg-zinc-900">
        <h1 class="text-center text-zinc-700 ">note me.</h1>
        <div class="form p-10 text-white">
            <form action="/create" method="POST">
                <input class=" rounded-lg block px-5 py-2 bg-zinc-800 " type="text" placeholder="Title goes here"
                    name="title">
                <textarea class="rounded-lg block px-5 py-2 mt-2 bg-zinc-800 resize-none " name="details"
                    placeholder="write you task details here" i></textarea>
                <input class="mt-2 text-zinc-800 bg-white rounded-lg px-2 py-1" type="submit" value="Create Task">
            </form>
        </div>
        <div class="tasks flex gap-3 flex-wrap p-10 ">
            <% if(files.length>0) { %>
                <% files.forEach(function(val){ %>
                    <div class="task min-w-72 px-3 py-4 bg-zinc-800 rounded-lg ">
                        <h1 class="text-white text-3xl tracking-tighter">
                            <%= val %>
                        </h1>
                        <div class="flex w-full justify-between items-center">
                            <a class="text-zinc-100 inline-block mt-3" href="/files/<%= val %>">Read More...</a>
                            <a class="text-zinc-100 inline-block mt-3 " href="/edit/<%= val %>">Edit filename</a>
                            <a class="text-zinc-100 inline-block mt-3 " href="/delete/<%= val %>">Delete</a>
                        </div>
                    </div>
                    <% }) %>
                        <% } else { %>
                            <h3 class="text-zinc-600">no tasks yet</h3>
                            <% } %>


        </div>
        <p class="fixed bottom-2 left-0 w-full text-center text-zinc-500 py-2 bg-zinc-900">
            @ankurbag
        </p>


    </div>
</body>

</html>